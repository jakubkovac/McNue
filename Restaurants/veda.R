veda <- function(){
  # url_veda <- "https://www.store.vedaveg.com/restauracja/veda-vegetarian-vegan-2#menu-denne-menu"
  # download.file(url_veda, destfile = "scrapedpage.html", quiet=TRUE)
  # raw <- read_html("scrapedpage.html")
  # 
  # 
  # jedlo <- html_nodes(raw, "span.muted") %>%
  #   html_text() %>% 
  #   tail(2)
  # 
  # jedlo <- str_split(jedlo, "polievka")
  #   
  # polievka <- jedlo[[1]][[2]]
  # jedlo <- map_chr(jedlo, ~.x[[1]])
  # 
  # jedlo <- str_trim(jedlo)
  # 
  # return(c("Veda",polievka, jedlo))
  stop()
}
